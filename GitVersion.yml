mode: ContinuousDeployment

branches:
  mychanges:
    regex: ^mychanges$
    mode: ContinuousDeployment
    tag: ''
    increment: Minor
    source-branches:
      - canary_old
      - misc.-fixes,-additions
